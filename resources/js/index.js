document.addEventListener('DOMContentLoaded', () => {

    initializeStorySlider();
    initializeNavigation();
});

// Keep only the necessary functions, remove generatePosts and initializeInfiniteScroll